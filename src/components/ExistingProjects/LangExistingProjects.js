const validationStatus = {
  id: 'existing.validationStatus',
  defaultMessage: 'Validation Status',
};
const challengeStatus = {
  id: 'existing.challengeStatus',
  defaultMessage: 'Challenge Status',
};

const projectText = { id: 'existing.project', defaultMessage: 'Project' };

const dataUploadText = {
  id: 'existing.dataUpload',
  defaultMessage: 'Data Upload',
};

const stageText = { id: 'existing.stage', defaultMessage: 'Stage' };

const validationOpen = {
  id: 'existing.validationOpen',
  defaultMessage: 'Validation Open',
};
const challengeOpen = {
  id: 'existing.challengeOpen',
  defaultMessage: 'Challenge Open',
};

const areaText = { id: 'existing.area', defaultMessage: 'Area, sq.km' };
const locationText = { id: 'existing.location', defaultMessage: 'Location' };

export {
  validationStatus,
  challengeStatus,
  projectText,
  dataUploadText,
  stageText,
  validationOpen,
  challengeOpen,
  areaText,
  locationText,
};
