const openText = { id: 'existing.status.open', defaultMessage: 'Open' };
const completedText = {
  id: 'existing.status.completed',
  defaultMessage: 'Completed',
};
const resolvedText = {
  id: 'existing.status.resolved',
  defaultMessage: 'Resolved',
};
const pendingText = {
  id: 'existing.status.pending',
  defaultMessage: 'Pending',
};
const unknownText = {
  id: 'existing.status.unknown',
  defaultMessage: 'Unknown',
};

export { openText, completedText, resolvedText, pendingText, unknownText };
